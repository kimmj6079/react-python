server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: let React Router (client-side) handle unknown paths.
        try_files $uri $uri/ /index.html;
    }
}
