# frontend Deployment 앞단의 ClusterIP Service. ingress.yaml이 `/` 경로
# 트래픽을 이 이름("frontend")으로 라우팅한다.
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: study-app
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
