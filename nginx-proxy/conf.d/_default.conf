# Host 헤더가 없거나(예: 컨테이너 자체 헬스체크의 wget http://localhost/) 등록된
# 어떤 앱의 server_name과도 매치되지 않는 요청을 처리하는 catch-all. 앱이 늘어나도
# "누가 default_server인지"는 항상 이 파일이 담당하므로 다른 conf 파일들은 신경 쓸
# 필요가 없다.
server {
    listen 80 default_server;
    server_name _;
    return 200 "ok\n";
}
